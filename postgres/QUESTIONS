other user info to store?
user schema (account names, pw protection and so on)
default to no network connection will need eventually though
granularity of BTC in transfer (currently microBTC is the smallest)
